<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/White10Black">

    <EditText
        android:id="@+id/search_global"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:padding="8dp"
        android:background="@color/White20Black"
        android:hint="Pesquisar"
        android:textColorHint="@color/textSecondary"
        android:gravity="center"
        android:drawableLeft="@drawable/ic_search"/>

    <android.support.v7.widget.RecyclerView
        android:id="@+id/lista_search_global"
        android:layout_below="@+id/search_global"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/row_lista_search_global"/>
</RelativeLayout>